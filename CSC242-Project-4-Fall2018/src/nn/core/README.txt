
You will need to implement core methods in PerceptronUnit.java and LogisticUnit.java.

You will also need to implement the core training algorithms, in SingleLayerFeedForwardNetwork.java (perceptron network update), and MultiLayerFeedForwardNetwork.java (backpropagation).

